mixin dropdown(text='Expand more', ...arr)
    .dropdown 
        .dropdown__body= text
            span(class='dropdown__arrow icon') expand_more

        .dropdown__content
            each item in arr 
                p= item
